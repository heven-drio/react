/* 入口js */

import React from 'react'
import ReactDOM from "react-dom"
import {Button} from "antd-mobile"  //从antd-mobile引入组件

ReactDOM.render(<Button type="primary">按钮</Button>,document.getElementById("root")) //渲染button
